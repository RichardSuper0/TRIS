// bootloader.java — Activity minimalissima
package tris; public class bootloader {
    // TODO: implementazione Activity
}
EOF cat > TRIS/BOOTLOADER/loader.cpp <<EOF
// loader.cpp — ponte Vulkan → ASM TODO: implementazione loader
EOF cat > TRIS/BOOTLOADER/CMakeLists.txt <<EOF
# CMakeLists — build per BOOTLOADER (opzionale) Puoi anche 
# ignorarlo se usi solo quello in TRIS/CMAKE
